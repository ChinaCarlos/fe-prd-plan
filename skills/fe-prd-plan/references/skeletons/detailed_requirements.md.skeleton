# 兼容说明

自 **0.2.0** 起，业务定稿文件名为 **`requirements.md`**，骨架见 [`requirements.md.skeleton`](requirements.md.skeleton)。

若用户或旧流程仍要求 `detailed_requirements.md`：按 `requirements.md.skeleton` 生成后**复制或另存**为 `detailed_requirements.md`，或只生成一份 `requirements.md` 并在对话中说明更名。

交互细规格请用 [`interaction.md.skeleton`](interaction.md.skeleton)，不要把全部 UI 细节只堆在本旧文件名里。
